/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;
import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class ReleasePlanningMeetingActionStateRepository implements Cloneable
{
private Vector<ReleasePlanningMeetingAction> actions;

public ReleasePlanningMeetingActionStateRepository()
{
actions = new Vector<ReleasePlanningMeetingAction>();
}

public Object clone() {
try {
ReleasePlanningMeetingActionStateRepository cl = (ReleasePlanningMeetingActionStateRepository) (super.clone());
Vector<ReleasePlanningMeetingAction> clonedActions = new Vector<ReleasePlanningMeetingAction>();
for (int i = 0; i < actions.size(); i++) {
clonedActions.add((ReleasePlanningMeetingAction)actions.elementAt(i).clone());
}
cl.actions = clonedActions;
return cl;
} catch (CloneNotSupportedException c) {
System.out.println(c.getMessage());
}
return null;
}

public boolean add(ReleasePlanningMeetingAction a)
{
if(actions.contains(a) == false)
{
actions.add(a);
return true;
}
return false;
}

public boolean remove(ReleasePlanningMeetingAction a)
{
if(actions.contains(a))
{
actions.remove(a);
return true;
}
return false;
}

public Vector<ReleasePlanningMeetingAction> getAllActions()
{
return actions;
}

public Vector<ReleasePlanningMeetingAction> getAllActions(SSObject a)
{
Vector<ReleasePlanningMeetingAction> all = new Vector<ReleasePlanningMeetingAction>();
for(int i=0; i<actions.size(); i++)
{
ReleasePlanningMeetingAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<ReleasePlanningMeetingAction> getAllActiveActions(SSObject a)
{
Vector<ReleasePlanningMeetingAction> all = new Vector<ReleasePlanningMeetingAction>();
for(int i=0; i<actions.size(); i++)
{
ReleasePlanningMeetingAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllActiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<ReleasePlanningMeetingAction> getAllInactiveActions(SSObject a)
{
Vector<ReleasePlanningMeetingAction> all = new Vector<ReleasePlanningMeetingAction>();
for(int i=0; i<actions.size(); i++)
{
ReleasePlanningMeetingAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllInactiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public ReleasePlanningMeetingAction getActionWithId(int id) {
for (int i = 0; i < actions.size(); i++) {
ReleasePlanningMeetingAction act = actions.get(i);
if (act.getId() == id) {
return act;
}
}
return null;
}

/*
* Replaces all the participants in each action with their equivalent objects
* in the current state. Calling this function solves the problem that
* happens when you clone actions -- their hashtables point to participant
* objects that were part of the previous, non-cloned state.
* Hence, this function should be called after this object is cloned.
*/
public void refetchParticipants(ArtifactStateRepository artifactRep, CustomerStateRepository customerRep, EmployeeStateRepository employeeRep, ProjectStateRepository projectRep, ToolStateRepository toolRep) {
for (int i = 0; i < actions.size(); i++) {
ReleasePlanningMeetingAction act = actions.elementAt(i);
act.refetchParticipants(artifactRep, customerRep, employeeRep, projectRep, toolRep);}
}
}