/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;
import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class GameOverActionStateRepository implements Cloneable
{
private Vector<GameOverAction> actions;

public GameOverActionStateRepository()
{
actions = new Vector<GameOverAction>();
}

public Object clone() {
try {
GameOverActionStateRepository cl = (GameOverActionStateRepository) (super.clone());
Vector<GameOverAction> clonedActions = new Vector<GameOverAction>();
for (int i = 0; i < actions.size(); i++) {
clonedActions.add((GameOverAction)actions.elementAt(i).clone());
}
cl.actions = clonedActions;
return cl;
} catch (CloneNotSupportedException c) {
System.out.println(c.getMessage());
}
return null;
}

public boolean add(GameOverAction a)
{
if(actions.contains(a) == false)
{
actions.add(a);
return true;
}
return false;
}

public boolean remove(GameOverAction a)
{
if(actions.contains(a))
{
actions.remove(a);
return true;
}
return false;
}

public Vector<GameOverAction> getAllActions()
{
return actions;
}

public Vector<GameOverAction> getAllActions(SSObject a)
{
Vector<GameOverAction> all = new Vector<GameOverAction>();
for(int i=0; i<actions.size(); i++)
{
GameOverAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<GameOverAction> getAllActiveActions(SSObject a)
{
Vector<GameOverAction> all = new Vector<GameOverAction>();
for(int i=0; i<actions.size(); i++)
{
GameOverAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllActiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<GameOverAction> getAllInactiveActions(SSObject a)
{
Vector<GameOverAction> all = new Vector<GameOverAction>();
for(int i=0; i<actions.size(); i++)
{
GameOverAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllInactiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public GameOverAction getActionWithId(int id) {
for (int i = 0; i < actions.size(); i++) {
GameOverAction act = actions.get(i);
if (act.getId() == id) {
return act;
}
}
return null;
}

/*
* Replaces all the participants in each action with their equivalent objects
* in the current state. Calling this function solves the problem that
* happens when you clone actions -- their hashtables point to participant
* objects that were part of the previous, non-cloned state.
* Hence, this function should be called after this object is cloned.
*/
public void refetchParticipants(ArtifactStateRepository artifactRep, CustomerStateRepository customerRep, EmployeeStateRepository employeeRep, ProjectStateRepository projectRep, ToolStateRepository toolRep) {
for (int i = 0; i < actions.size(); i++) {
GameOverAction act = actions.elementAt(i);
act.refetchParticipants(artifactRep, customerRep, employeeRep, projectRep, toolRep);}
}
}