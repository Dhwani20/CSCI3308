/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;
import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class SetEmployeeProductivitiesActionStateRepository implements Cloneable
{
private Vector<SetEmployeeProductivitiesAction> actions;

public SetEmployeeProductivitiesActionStateRepository()
{
actions = new Vector<SetEmployeeProductivitiesAction>();
}

public Object clone() {
try {
SetEmployeeProductivitiesActionStateRepository cl = (SetEmployeeProductivitiesActionStateRepository) (super.clone());
Vector<SetEmployeeProductivitiesAction> clonedActions = new Vector<SetEmployeeProductivitiesAction>();
for (int i = 0; i < actions.size(); i++) {
clonedActions.add((SetEmployeeProductivitiesAction)actions.elementAt(i).clone());
}
cl.actions = clonedActions;
return cl;
} catch (CloneNotSupportedException c) {
System.out.println(c.getMessage());
}
return null;
}

public boolean add(SetEmployeeProductivitiesAction a)
{
if(actions.contains(a) == false)
{
actions.add(a);
return true;
}
return false;
}

public boolean remove(SetEmployeeProductivitiesAction a)
{
if(actions.contains(a))
{
actions.remove(a);
return true;
}
return false;
}

public Vector<SetEmployeeProductivitiesAction> getAllActions()
{
return actions;
}

public Vector<SetEmployeeProductivitiesAction> getAllActions(SSObject a)
{
Vector<SetEmployeeProductivitiesAction> all = new Vector<SetEmployeeProductivitiesAction>();
for(int i=0; i<actions.size(); i++)
{
SetEmployeeProductivitiesAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<SetEmployeeProductivitiesAction> getAllActiveActions(SSObject a)
{
Vector<SetEmployeeProductivitiesAction> all = new Vector<SetEmployeeProductivitiesAction>();
for(int i=0; i<actions.size(); i++)
{
SetEmployeeProductivitiesAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllActiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<SetEmployeeProductivitiesAction> getAllInactiveActions(SSObject a)
{
Vector<SetEmployeeProductivitiesAction> all = new Vector<SetEmployeeProductivitiesAction>();
for(int i=0; i<actions.size(); i++)
{
SetEmployeeProductivitiesAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllInactiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public SetEmployeeProductivitiesAction getActionWithId(int id) {
for (int i = 0; i < actions.size(); i++) {
SetEmployeeProductivitiesAction act = actions.get(i);
if (act.getId() == id) {
return act;
}
}
return null;
}

/*
* Replaces all the participants in each action with their equivalent objects
* in the current state. Calling this function solves the problem that
* happens when you clone actions -- their hashtables point to participant
* objects that were part of the previous, non-cloned state.
* Hence, this function should be called after this object is cloned.
*/
public void refetchParticipants(ArtifactStateRepository artifactRep, CustomerStateRepository customerRep, EmployeeStateRepository employeeRep, ProjectStateRepository projectRep, ToolStateRepository toolRep) {
for (int i = 0; i < actions.size(); i++) {
SetEmployeeProductivitiesAction act = actions.elementAt(i);
act.refetchParticipants(artifactRep, customerRep, employeeRep, projectRep, toolRep);}
}
}