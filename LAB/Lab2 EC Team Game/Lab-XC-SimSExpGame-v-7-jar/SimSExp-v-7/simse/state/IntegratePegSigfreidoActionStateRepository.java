/* File generated by: simse.codegenerator.stategenerator.RepositoryGenerator */
package simse.state;
import simse.adts.objects.*;
import simse.adts.actions.*;
import java.util.*;

public class IntegratePegSigfreidoActionStateRepository implements Cloneable
{
private Vector<IntegratePegSigfreidoAction> actions;

public IntegratePegSigfreidoActionStateRepository()
{
actions = new Vector<IntegratePegSigfreidoAction>();
}

public Object clone() {
try {
IntegratePegSigfreidoActionStateRepository cl = (IntegratePegSigfreidoActionStateRepository) (super.clone());
Vector<IntegratePegSigfreidoAction> clonedActions = new Vector<IntegratePegSigfreidoAction>();
for (int i = 0; i < actions.size(); i++) {
clonedActions.add((IntegratePegSigfreidoAction)actions.elementAt(i).clone());
}
cl.actions = clonedActions;
return cl;
} catch (CloneNotSupportedException c) {
System.out.println(c.getMessage());
}
return null;
}

public boolean add(IntegratePegSigfreidoAction a)
{
if(actions.contains(a) == false)
{
actions.add(a);
return true;
}
return false;
}

public boolean remove(IntegratePegSigfreidoAction a)
{
if(actions.contains(a))
{
actions.remove(a);
return true;
}
return false;
}

public Vector<IntegratePegSigfreidoAction> getAllActions()
{
return actions;
}

public Vector<IntegratePegSigfreidoAction> getAllActions(SSObject a)
{
Vector<IntegratePegSigfreidoAction> all = new Vector<IntegratePegSigfreidoAction>();
for(int i=0; i<actions.size(); i++)
{
IntegratePegSigfreidoAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<IntegratePegSigfreidoAction> getAllActiveActions(SSObject a)
{
Vector<IntegratePegSigfreidoAction> all = new Vector<IntegratePegSigfreidoAction>();
for(int i=0; i<actions.size(); i++)
{
IntegratePegSigfreidoAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllActiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public Vector<IntegratePegSigfreidoAction> getAllInactiveActions(SSObject a)
{
Vector<IntegratePegSigfreidoAction> all = new Vector<IntegratePegSigfreidoAction>();
for(int i=0; i<actions.size(); i++)
{
IntegratePegSigfreidoAction b = actions.elementAt(i);
Vector<SSObject> parts = b.getAllInactiveParticipants();
for(int j=0; j<parts.size(); j++)
{
if(parts.elementAt(j).equals(a))
{
all.add(b);
break;
}
}
}
return all;
}

public IntegratePegSigfreidoAction getActionWithId(int id) {
for (int i = 0; i < actions.size(); i++) {
IntegratePegSigfreidoAction act = actions.get(i);
if (act.getId() == id) {
return act;
}
}
return null;
}

/*
* Replaces all the participants in each action with their equivalent objects
* in the current state. Calling this function solves the problem that
* happens when you clone actions -- their hashtables point to participant
* objects that were part of the previous, non-cloned state.
* Hence, this function should be called after this object is cloned.
*/
public void refetchParticipants(ArtifactStateRepository artifactRep, CustomerStateRepository customerRep, EmployeeStateRepository employeeRep, ProjectStateRepository projectRep, ToolStateRepository toolRep) {
for (int i = 0; i < actions.size(); i++) {
IntegratePegSigfreidoAction act = actions.elementAt(i);
act.refetchParticipants(artifactRep, customerRep, employeeRep, projectRep, toolRep);}
}
}